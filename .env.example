SERVER="your-ssh-alias"            # SSH alias defined in ~/.ssh/config
IMAGE_NAME="spektral-ui-doc"       # Commit tag will be added to the name during deployment
CONTAINER_NAME="spektral-ui-doc"   # Container name in docker
DOCKERFILE="frontend/Dockerfile"   # Dockerfile location
NETWORK="my-network"               # Docker network (Caddy)
PORT="0000"                        # Host:Container port mapping
URL="https://website.io"           # Project URL
